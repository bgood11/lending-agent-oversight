import { FileReviewWorkspace } from "@/components/principal/file-review-workspace";

export const metadata = {
  title: "File reviews — Lending Agent Oversight",
};

export default function ReviewsPage() {
  return <FileReviewWorkspace />;
}
